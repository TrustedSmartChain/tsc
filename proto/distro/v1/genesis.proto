syntax = "proto3";
package distro.v1;

import "gogoproto/gogo.proto";
import "amino/amino.proto";

option go_package = "github.com/TrustedSmartChain/tsc/x/distro/types";

// GenesisState defines the module genesis state
message GenesisState {
  // Params defines all the parameters of the module.
  Params params = 1 [(gogoproto.nullable) = false];
}

// Params defines the set of module parameters.
message Params {
  option (amino.name) = "tsc/x/distro/params";
  option (gogoproto.equal) = true;

  string mintingAddress = 1 [(gogoproto.moretags) = "json:\"minting_address\""];
  string receivingAddress = 2 [(gogoproto.moretags) = "json:\"receiving_address\""];
  string denom = 3 [(gogoproto.moretags) = "json:\"denom\""];
  string maxSupply = 4 [(gogoproto.moretags) = "json:\"max_supply\""];
  string distributionStartDate = 5 [(gogoproto.moretags) = "json:\"distribution_start_date\""];
  uint64 monthsInHalvingPeriod = 6 [(gogoproto.moretags) = "json:\"months_in_halving_period\""];
}